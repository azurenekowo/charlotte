---
import { invoke } from "../../../api/getmeta";
import Layout from "../../../layouts/Layout.astro";
import NotFound from "../../../components/404.astro";
import DJMeta from "../../../components/DJMeta.astro";

let { success, data } = await invoke(Astro.params.q);
---

<Layout title="random doujin">
  {success ? <DJMeta data={data} /> : <NotFound />}
</Layout>
